{
  "name": "@sage/agents",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @sage/shared build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @sage/shared build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sage/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
